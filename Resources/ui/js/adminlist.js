var kunstmaanbundles = kunstmaanbundles || {};

kunstmaanbundles.adminlist = (function($, window, undefined) {

    var init;

    init = function() {

    };

    return {
        init: init
    };

}(jQuery, window));

$(function() {
    kunstmaanbundles.adminlist.init();
});
